from django.urls import include, path

from . import views

urlpatterns = [
    # path("<int:race_id>/", views.user_signup_view, name="material"),
    # path("<int:material_id>/", views.user_signup_view, name="race"),
    # path("<int:item_id>/", views.user_signup_view, name="item"),
]
